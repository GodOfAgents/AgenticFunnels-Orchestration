# =============================================================================
# AFO Platform - Backend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# cp .env.example .env

# =============================================================================
# CORE CONFIGURATION
# =============================================================================
ENVIRONMENT=development
HOST=0.0.0.0
PORT=8001

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# MongoDB Connection
MONGO_URL=mongodb://localhost:27017/afo_db

# Redis Connection (for caching and real-time features)
REDIS_URL=redis://localhost:6379

# Milvus Vector Database (for RAG/semantic search)
MILVUS_HOST=localhost
MILVUS_PORT=19530

# =============================================================================
# SECURITY & AUTHENTICATION
# =============================================================================
# Secret key for JWT tokens (generate a secure random string)
# SECRET_KEY=your-secret-key-here

# JWT expiration time (in minutes)
# JWT_EXPIRATION_MINUTES=1440

# CORS allowed origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# =============================================================================
# LLM INTEGRATION (Optional - for platform features)
# =============================================================================
# Emergent LLM Key (universal key for OpenAI, Anthropic, Google)
# Only needed if using platform's built-in LLM features
# EMERGENT_LLM_KEY=your_emergent_key_here

# =============================================================================
# PHASE 2: VOICE & WORKFLOWS (User-Provided Keys)
# =============================================================================
# Note: These are NOT required here. Users provide their own keys via the UI.
# The platform stores them securely per-tenant.

# LiveKit Configuration (Open-source WebRTC)
LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=devsecret

# Example: If you want to test voice features during development
# DEEPGRAM_API_KEY=your_deepgram_key_here
# ELEVENLABS_API_KEY=your_elevenlabs_key_here
# OPENAI_API_KEY=your_openai_key_here

# =============================================================================
# CALENDAR INTEGRATION (User-Provided)
# =============================================================================
# Google Calendar - Users provide OAuth2 credentials via UI
# GOOGLE_CLIENT_ID=your_google_client_id
# GOOGLE_CLIENT_SECRET=your_google_client_secret

# Calendly - Users provide API keys via UI
# CALENDLY_API_KEY=your_calendly_api_key

# =============================================================================
# CRM & WEBHOOKS (User-Provided)
# =============================================================================
# Users configure webhook URLs and authentication in the platform UI
# No environment variables needed here

# =============================================================================
# DAILY.CO (WebRTC for Voice)
# =============================================================================
# Daily.co API key (optional - for creating rooms programmatically)
# DAILY_API_KEY=your_daily_api_key

# =============================================================================
# MONITORING & LOGGING
# =============================================================================
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Enable detailed logging
VERBOSE_LOGGING=false

# =============================================================================
# EXTERNAL API TIMEOUTS
# =============================================================================
# HTTP request timeout (in seconds)
HTTP_TIMEOUT=30

# WebSocket connection timeout (in seconds)
WS_TIMEOUT=60

# =============================================================================
# PERFORMANCE TUNING
# =============================================================================
# Maximum concurrent voice sessions
MAX_VOICE_SESSIONS=50

# Maximum workflow executions per minute
WORKFLOW_RATE_LIMIT=100

# Redis connection pool size
REDIS_POOL_SIZE=10

# MongoDB connection pool size
MONGO_POOL_SIZE=10

# =============================================================================
# FEATURE FLAGS
# =============================================================================
# Enable/disable specific features
ENABLE_VOICE_FEATURES=true
ENABLE_WORKFLOW_FEATURES=true
ENABLE_ANOMALY_DETECTION=true
ENABLE_ANALYTICS=true

# =============================================================================
# DEVELOPMENT ONLY
# =============================================================================
# Enable auto-reload on code changes
AUTO_RELOAD=true

# Enable API documentation (Swagger UI)
ENABLE_DOCS=true

# Enable debug mode
DEBUG=false

# =============================================================================
# NOTES
# =============================================================================
# 1. Never commit this file with actual credentials to version control
# 2. Use strong, unique passwords for production
# 3. Rotate API keys regularly
# 4. The BYOK model means most API keys are provided by users via UI
# 5. See README.md for detailed setup instructions
